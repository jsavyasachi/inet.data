Independent diff review found that the public API docstring audit is incomplete and sometimes violates the task's exactness requirement. Re-audit every public defn/defmacro in the originally allowed files; make docs-only changes and do not alter behavior.

Concrete misses to fix include: all public special-use predicates (`private?` through `global?`) still omit accepted input representations and lenient false/no-throw behavior; `domain-byte-seq`, `format.flat/load`, `format.psl/load`, `ubyte`, `sbyte`, and `bytes-hash-code` use vague phrases such as “underlying exception” instead of naming exact exception types; several failure modes remain unstated (for example numeric/index failures in `address-add`, and validate the actual nil/throw behavior in `format.flat/domain-etld` and `domain-e2ld` rather than claiming `DNSDomainException`). Probe implementations where needed. The requirement is: each public function must state accepted input types, strict versus lenient behavior, exact exception type(s) on failure if any, and Big-O for collection-processing functions. Do not guess; if Java/Clojure can throw multiple documented types, enumerate them accurately. Preserve all existing docstring content while strengthening it.

After repair, run `clojure -T:build compile-java && clojure -M:test`, force-require every touched namespace under `*warn-on-reflection* true`, confirm no behavior/body/dependency/release/build changes, and leave everything unstaged/uncommitted. End with the same required JSON object.
