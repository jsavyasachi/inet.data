# Codex task: iana-registry

You are an implementation worker in `/Users/savya/projects/inet.data/.worktrees/iana-registry` on branch `feat/iana-registry`, baseline `c82bd894c7b0be67603c0ea5698da8bfa932bf3e`. This mature Clojure library has IP/DNS deftypes, generated Java parsers, Clojure wrappers under `src/clojure/inet/data/`, and resources on the classpath. Preserve behavior. Do not commit, push, deploy, use credentials, use destructive git commands, or write outside this worktree.

In `src/clojure/inet/data/ip.clj`, `special-use-blocks` is a hardcoded map of IANA special-purpose IPv4/IPv6 blocks (RFC 1918/6890/4193/3927/6598 etc.). Do the reachable, honest scoped-down implementation: extract that data into a documented, versioned EDN resource such as `resources/inet/data/special-use-registry.edn`, including a metadata/header entry that cites the exact IANA registry names/RFCs and a truthful `:registry-version` or `:generated`/snapshot date. Load it at namespace load using `clojure.edn/read` plus `clojure.java.io/resource`. Preserve the existing public behavior and data meanings. Test resource loading and classification behavior first.

Do not fabricate or claim a live IANA fetch or generated-at-build pipeline. Do not use network access. If a truthful snapshot/version cannot be established from repository evidence, either document it explicitly as a repository snapshot tied to this commit/date without claiming live freshness, or report a concrete blocker rather than overclaiming. No unrelated changes.

Use strict TDD: red focused test first, then minimum implementation, with red evidence in narration. Every touched Clojure source must retain `(set! *warn-on-reflection* true)` and compile with zero reflection warnings. CRITICAL: run `clojure -T:build compile-java` before `clojure -M:test`, then require the full suite green and zero reflection warnings.

Do not add Maven dependencies; if one seems needed, do not add it silently and list it in `unresolved_blockers`. Leave changes unstaged and uncommitted. End with only the required JSON object containing `summary`, `files_changed`, `tests_run`, `reflection_warnings`, `new_dependencies_added`, and `unresolved_blockers`.
