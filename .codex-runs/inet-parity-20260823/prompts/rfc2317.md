# Codex task: rfc2317

You are an implementation worker in `/Users/savya/projects/inet.data/.worktrees/rfc2317` on branch `feat/rfc2317`, baseline `c82bd894c7b0be67603c0ea5698da8bfa932bf3e`. This mature Clojure IP/DNS data-types library uses `IPAddress`/`IPNetwork`/`Domain` deftypes and generated Java parsers; Clojure wrappers are under `src/clojure/inet/data/`. Preserve all existing behavior. Do not commit, push, deploy, use credentials, use destructive git commands, or write outside this worktree.

`src/clojure/inet/data/arpa.clj` has a documented deliberate decision not to implement RFC 2317 classless IPv4 reverse-DNS delegation. Do not change existing `ip->domain` or `domain->ip` behavior or tests. Add separately named opt-in functions (choose clear non-colliding names, such as `classless-ip->domain` and `classless-domain->ip`) implementing RFC 2317 both ways. At minimum support the representative delegation `192.0.2.0/26` as `0/26.2.0.192.in-addr.arpa`, using existing IP/network/domain types and conventions. Specify and test accepted prefix ranges, invalid input, round trips, and exception behavior. The change must be strictly additive and every existing test must pass unmodified.

Use strict TDD: focused red tests first, then minimum implementation, preserving red evidence in narration. Every touched Clojure source file must retain `(set! *warn-on-reflection* true)` and compile with zero reflection warnings. CRITICAL: run `clojure -T:build compile-java` before `clojure -M:test`; otherwise generated parser classes can be missing/stale. Finish with both commands green and zero reflection warnings.

Do not add Maven dependencies. If one seems necessary, do not add it silently; report it in `unresolved_blockers`. Leave changes unstaged and uncommitted. End with only the required JSON object containing `summary`, `files_changed`, `tests_run` entries (`command`, `exit_code`, `result`), integer `reflection_warnings`, `new_dependencies_added`, and `unresolved_blockers`.
