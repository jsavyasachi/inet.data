# Codex task: psl-management

You are an implementation worker in `/Users/savya/projects/inet.data/.worktrees/psl-management` on branch `feat/psl-management`, baseline `c82bd894c7b0be67603c0ea5698da8bfa932bf3e`. This mature Clojure library has format helpers under `src/clojure/inet/data/format/`. Preserve all existing behavior. Do not commit, push, deploy, use credentials, use destructive git commands, or write outside this worktree.

First inspect `src/clojure/inet/data/format/psl.clj` and verify its current behavior: it reportedly memoizes one process-lifetime network fetch of the Public Suffix List. Check the existing PSL URL and format before designing anything, and reuse that exact format. Add: (a) a bundled/vendored PSL snapshot resource so a fresh process has a working default with no network call, (b) an explicit refresh function that re-fetches and updates the cache, (c) a configurable network timeout, and (d) last-known-good fallback (bundled or previously fetched) when refresh fails, instead of throwing or returning nothing. Tests must be deterministic and not depend on live network; use dependency seams/redefs consistent with the codebase. Preserve compatibility where reasonable and precisely document cache/refresh semantics. Do not overclaim snapshot freshness; record its source/date truthfully.

Use strict TDD with focused red tests first for bundled default, successful refresh, timeout configuration, and failed-refresh fallback; then implement minimally and retain red evidence. Every touched Clojure source must retain `(set! *warn-on-reflection* true)` and compile with zero reflection warnings. CRITICAL: run `clojure -T:build compile-java` before `clojure -M:test`; require both green with zero reflection warnings.

Do not add Maven dependencies. If one seems necessary, leave it out and report it in `unresolved_blockers`. Do not silently fetch data from the network; if the repo already contains no snapshot and you cannot truthfully vendor a valid one without network, report that limitation honestly rather than inventing data. Leave changes unstaged and uncommitted. End with only the required JSON object containing `summary`, `files_changed`, `tests_run`, `reflection_warnings`, `new_dependencies_added`, and `unresolved_blockers`.
