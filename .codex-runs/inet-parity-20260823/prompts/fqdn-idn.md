# Codex task: fqdn-idn

You are an implementation worker in `/Users/savya/projects/inet.data/.worktrees/fqdn-idn` on branch `feat/fqdn-idn`, baseline `c82bd894c7b0be67603c0ea5698da8bfa932bf3e`. This mature Clojure IP/DNS data-types library uses `Domain` deftypes and generated Java parsers. Preserve behavior. Do not commit, push, deploy, use credentials, use destructive git commands, write outside this worktree, or add dependencies.

This finding is risky and must be honestly scoped. Before code, use a Clojure/Java REPL probe to establish whether `java.net.IDN/toASCII` and `toUnicode` suffice for zero-dependency conversion and document that Java stdlib provides IDNA2003, not IDNA2008/UTS#46. Do not add ICU4J or any Maven dependency. Scope to: (a) inspect current absolute-domain/trailing-dot behavior in `src/clojure/inet/data/dns.clj` via a probe before assuming it is broken; a `Domain` for `"example.com."` should compare and round-trip distinctly from `"example.com"`; if already correct, do not invent a fix and report it as already correct; if genuinely incorrect, add the smallest tested fix. (b) add public `idn->ascii` and `ascii->idn` wrappers around `java.net.IDN`, with precise docstrings stating IDNA2003-only and not IDNA2008/UTS#46. Test Unicode/ASCII round trips, normalization, trailing dots, invalid input, and existing type conventions.

Use strict TDD: probe first, then focused red tests before behavior changes, minimum implementation, and retain evidence in narration. Every touched Clojure source must retain `(set! *warn-on-reflection* true)` and compile with zero reflection warnings. CRITICAL: `clojure -T:build compile-java` must precede `clojure -M:test`; require both green and zero reflection warnings.

Do not add any Maven dependency. If you believe one is required for standards beyond IDNA2003, leave it out and state the limitation/blocker explicitly. Leave changes unstaged and uncommitted. End with only the required JSON object containing `summary`, `files_changed`, `tests_run`, `reflection_warnings`, `new_dependencies_added`, and `unresolved_blockers`.
