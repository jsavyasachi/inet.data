# Codex task: network-set-perf

You are an implementation worker in `/Users/savya/projects/inet.data/.worktrees/network-set-perf` on branch `feat/network-set-perf`, baseline `c82bd894c7b0be67603c0ea5698da8bfa932bf3e`. This mature Clojure library defines `aggregate-networks` in `src/clojure/inet/data/ip.clj`. Preserve correctness and existing behavior. Do not commit, push, deploy, use credentials, use destructive git commands, or write outside this worktree.

Benchmark `aggregate-networks` and network-set construction against a reproducible large synthetic input (about 50k-100k random prefixes) using only `System/nanoTime`; do not add criterium or another benchmark dependency. Keep the benchmark checked in as an explicitly opt-out `^:integration`-tagged or equivalent slow test so normal runs remain suitable, while documenting how to invoke it. Use measurements/profiling evidence to identify any actual quadratic scan or repeated-parsing hot spot. Fix only a hot spot the benchmark proves; do not speculate. If performance is already reasonable at that scale, report it honestly and ship the benchmark/tests without inventing an optimization. Set a robust, non-flaky regression criterion appropriate to variable CI rather than a brittle absolute microsecond threshold. Preserve semantic tests.

Use TDD for any behavior/performance guard: add/run the benchmark or focused test first, capture baseline evidence, then make only an evidence-backed implementation change and rerun. Every touched Clojure source must retain `(set! *warn-on-reflection* true)` and compile with zero reflection warnings. CRITICAL: run `clojure -T:build compile-java` before `clojure -M:test`; full suite must be green with zero reflection warnings. Also run the opt-in benchmark explicitly and report input size/timing.

Do not add Maven dependencies. If one seems necessary, omit it and report it in `unresolved_blockers`. Leave changes unstaged and uncommitted. End with only the required JSON object containing `summary`, `files_changed`, `tests_run`, `reflection_warnings`, `new_dependencies_added`, and `unresolved_blockers`.
