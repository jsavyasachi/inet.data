# Codex task: ecosystem-integrations

You are an implementation worker in `/Users/savya/projects/inet.data/.worktrees/ecosystem-integrations` on branch `feat/ecosystem-integrations`, baseline `c82bd894c7b0be67603c0ea5698da8bfa932bf3e`. This mature Clojure IP/DNS library has optional serialization backends under `src/clojure/inet/data/s11n/`. Preserve core loading without optional dependencies. Do not commit, push, deploy, use credentials, use destructive git commands, or write outside this worktree.

Add optional Transit and Nippy serialization support for `IPAddress`, `IPNetwork`, and `Domain`. First inspect and mirror exactly the optional-dependency and guarded-require patterns used by `src/clojure/inet/data/s11n/byteable.clj`, `s11n/abracad.clj`, and current `deps.edn`. Add `com.cognitect/transit-clj` and `com.taoensso/nippy` as `:optional true` dependencies in the same project pattern and add them to the `:test` alias `:extra-deps` so tests exercise round trips. Core library namespaces must still load when these deps are absent. Implement idiomatic backend handlers/extensions, stable type tags, and round-trip tests for all three types and representative IPv4/IPv6/network/domain values. Do not implement `datafy` or `print-dup`; explicitly note that out-of-scope item in `unresolved_blockers` only if useful.

Use strict TDD: add focused tests first and run red, then minimum backend code; retain red evidence. Every touched Clojure source must retain `(set! *warn-on-reflection* true)` and compile with zero reflection warnings. CRITICAL: `clojure -T:build compile-java` before `clojure -M:test`; require full green and zero reflection warnings.

The two named dependencies are explicitly authorized. Add no other Maven dependency. Report every newly added dependency honestly in `new_dependencies_added`; any other desired dependency must not be added and must be listed in `unresolved_blockers`. Leave changes unstaged and uncommitted. End with only the required JSON object containing `summary`, `files_changed`, `tests_run`, `reflection_warnings`, `new_dependencies_added`, and `unresolved_blockers`.
