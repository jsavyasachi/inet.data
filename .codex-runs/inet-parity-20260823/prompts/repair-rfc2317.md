Independent standards review found a concrete scope bug. RFC 2317 states this technique is for non-octet-boundary address spaces covering fewer than 256 addresses. In this API's `first-octet/prefix` label design beneath a `/24` reverse zone, the valid classless prefixes are `/25` through `/31`; `/1` through `/24` are not valid encodings for the four-label form currently emitted, and `/32` is octet-aligned and remains covered by the existing non-classless API. Your implementation currently accepts and tests `/1..31`.

Repair only this defect: restrict both directions to `/25..31`, update docstrings, and change the tests so `/25..31` round-trip while `/0..24` and `/32` are rejected. Preserve the representative `192.0.2.0/26 -> 0/26.2.0.192.in-addr.arpa`, preserve strict additivity and all existing default behavior, and add no dependency. Use a red/green regression step for the newly rejected range, then run `clojure -T:build compile-java && clojure -M:test` with zero reflection warnings. Do not commit. End with the required JSON object.
