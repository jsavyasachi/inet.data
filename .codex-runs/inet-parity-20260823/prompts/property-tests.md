# Codex task: property-tests

You are a TEST-ONLY worker in `/Users/savya/projects/inet.data/.worktrees/property-tests` on branch `feat/property-tests`, baseline `c82bd894c7b0be67603c0ea5698da8bfa932bf3e`. This mature Clojure IP/DNS library uses generated Java parsers and has an existing example-based test suite. Do not change any file under `src/`. Do not commit, push, deploy, use credentials, use destructive git commands, or write outside this worktree.

Inspect the existing test suite first so generators encode real library invariants. Add `org.clojure/test.check` only to the `:test` alias's `:extra-deps` if it is not already present; nowhere else. Add generative tests for: IPv4 and IPv6 address parsing round trips (`parse -> string -> parse` equality), canonical string representation stability, domain normalization idempotence, `aggregate-networks` invariants (the aggregate covers exactly the same address space as inputs, neither more nor less), and malformed input behavior (garbage never throws an unexpected exception type and consistently uses the library's typed exceptions). Use bounded deterministic/reproducible generation and avoid astronomically enumerating IPv6 address spaces. Do not weaken, delete, skip, or modify existing example tests unless required only to share harmless helpers.

Follow test-first discipline: add each property and run it to expose any real counterexample before changing anything else; this worker is forbidden from implementation fixes, so report discovered product bugs as blockers with seed/minimal case. Any touched Clojure test file should compile with zero reflection warnings; do not remove warning settings. CRITICAL: run `clojure -T:build compile-java` before `clojure -M:test`; require full suite green with zero reflection warnings unless a genuine counterexample is reported.

Only `org.clojure/test.check` is authorized if absent. Add no other Maven dependency; report all added deps in `new_dependencies_added`. Leave changes unstaged and uncommitted. End with only the required JSON object containing `summary`, `files_changed`, `tests_run`, `reflection_warnings`, `new_dependencies_added`, and `unresolved_blockers`.
